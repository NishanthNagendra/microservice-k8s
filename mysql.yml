apiVersion: v1
kind: Pod
metadata: 
  name: mysql-pod
  labels:
    app: mysql
spec:
  containers:
  - name: mysql
    image: mysql:5.7
    ports:
    - name: mysql-port
      containerPort: 3306
    env:
      - name: username
        valueFrom:
          secretKeyRef:
            name: username
            key: username
      - name: password
        valueFrom:
          secretKeyRef:
            name: password
            key: password
      - name: rootPassword
        valueFrom:
          secretKeyRef:
            name: rootPassword
            key: rootPassword
      - name: database
        valueFrom:
          secretKeyRef:
            name: database
            key: database
